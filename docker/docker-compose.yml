version: '1.0'

services:
  redis:
    image: redis:7-alpine
    container_name: inkguess-redis
    ports: 
     - "6379:6379"
    volumes:
     - redis_data:/data
    restart: always

volumes:
  redis_data: